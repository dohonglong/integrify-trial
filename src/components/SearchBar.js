import React from "react";

const SearchBar = () => {

    return (

        <form>
            <input
                type="text" 
                placeholder="Search here" 
            />
            <button>Search</button>
        </form>
    )
}

export default SearchBar;